# 数据服务批量审批与增量授权功能需求文档

## 一、需求背景

### 1.1 现状分析

**当前提单流程**：
1. 用户创建或更新API，生成新版本（未提单状态）
2. 用户选择API，填写审批单信息（包括授权用户、授权期限等）
3. 系统向DM发起提单
4. 用户手动调用刷新接口，检查审批状态
5. 审批通过后，用户在页面点击刷新按钮，调用 `approvalRefresh` 接口生成Token（已实现）

**当前存在的问题**：
1. API可被重复申请，但每次申请都需要填写全部的授权用户
2. 如果一个API已经审批通过，需要新增授权用户时，用户需要：
    - 更新API创建新版本
    - 重新填写所有授权用户（包括已有的和新增的）
    - 重新提交审批
3. 用户体验差，操作繁琐，容易遗漏用户
4. 审批通过后，没有自动生成Token的机制

### 1.2 需求目标

实现数据服务的**增量授权功能**：
1. 支持批量选择单个或多个API进行提单
2. **用户只需填写新增的授权用户即可，系统自动合并历史用户**
3. 在审批通过后生成Token时，系统自动查询历史授权用户并合并
4. 保留历史用户的配置信息（IP 白名单、访问限制等）
5. 审批通过后，用户点击"刷新状态"按钮，系统自动为所有用户生成Token

## 二、功能概述

### 2.1 已实现功能

#### 2.1.1 批量提单接口
- **接口路径**：`POST /dss/apiservice/submit`
- **实现位置**：`ApiServiceCoreRestfulApi.java:190-243`
- **功能描述**：
    - ✅ 支持批量选择多个API进行提单
    - ✅ 填写审批单名称、授权用户、授权期限等信息
    - ✅ 支持JDBC API的特殊处理（开发负责人、产品信息）
    - ✅ 支持永久授权（duration="*"）

#### 2.1.2 查询可提单API
- **接口路径**：`GET /dss/apiservice/availableSubmitApi`
- **实现位置**：`ApiServiceCoreRestfulApi.java:251-265`
- **功能描述**：
    - 返回最新版本为"未提单"状态的API
    - 仅返回属于当前工作空间的API

#### 2.1.3 审批状态刷新接口
- **接口路径**：`GET /dss/apiservice/approval/refresh`
- **实现位置**：`ApprovalServiceImpl.refreshStatus()`
- **功能描述**：
    - 请求DM获取审批单状态
    - 更新本地审批单状态
    - 审批通过后触发监听器 `afterApprovalSuccess()`

#### 2.1.4 提单核心逻辑
- **实现位置**：`ApiServiceImpl.submitToDm()`（第212-235行）
- **功能描述**：
    - 生成统一审批单号（UUID）
    - 每个API版本创建一条审批记录
    - 更新API版本状态为"已提单"
    - 调用DM提单接口

#### 2.1.5 审批成功回调
- **实现位置**：`ApiServiceImpl.afterApprovalSuccess()`（第238-251行）
- **功能描述**：
    - ✅ 禁用历史版本（状态改为禁用）
    - ✅ 禁用历史版本Token
    - ✅ 将当前版本改为运行中

#### 2.1.6 审批刷新与Token生成接口（核心功能）
- **接口路径**：`GET /dss/apiservice/approvalRefresh`
- **实现位置**：`ApiServiceTokenRestfulApi.refresh()`（第106-135行）
- **功能描述**：
    - 请求DM获取审批单状态
    - 更新本地审批单状态
    - 审批通过后，自动调用 Token 生成逻辑
    - **自动合并历史授权用户**（增量授权核心）
    - 为所有用户（申请用户 + 历史用户）生成Token
    - 禁用旧版本Token
    - 仅允许审批单的创建者调用此接口

### 2.2 增量授权核心功能

**功能描述**：
- ✅ 自动增量授权：系统在 Token 生成时自动查询历史审批单，合并历史用户
- ✅ 保留历史配置：保留历史用户的 IP 白名单、访问限制等配置
- ✅ 无需用户干预：用户无需填写任何额外参数，系统自动处理
- ✅ 版本隔离：每个 API 版本有独立的 Token 集合
- ✅ Token 状态管理：新版本审批通过后自动禁用旧版本 Token

## 三、数据模型分析

### 3.1 现有表结构

#### 3.1.1 dss_apiservice_api（API配置表）
```sql
CREATE TABLE `dss_apiservice_api` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `name` varchar(180) NOT NULL COMMENT '服务名称',
  `alias_name` varchar(200) NOT NULL COMMENT '服务中文名称',
  `status` int(11) DEFAULT '0' COMMENT '服务状态，0停止，1运行中，2删除',
  -- ... 其他字段
  PRIMARY KEY (`id`)
) ENGINE=InnoDB;
```

#### 3.1.2 dss_apiservice_api_version（API版本表）
```sql
CREATE TABLE `dss_apiservice_api_version` (
   `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
   `api_id` bigint(20) NOT NULL COMMENT '服务的ID',
   `version` varchar(50) NOT NULL COMMENT '版本信息',
   `status` tinyint(1) DEFAULT '1' COMMENT '0禁用，1运行中，2未提单，3已提单',
   `auth_id` varchar(200) COMMENT 'DM审批单号',
   -- ... 其他字段
   PRIMARY KEY(`id`)
) ENGINE=InnoDB;
```

**版本状态说明**：
- `0` (DISABLE): 禁用 - 历史版本被新版本替代
- `1` (ENABLE): 运行中 - 当前生效的版本
- `2` (NOT_SUBMITTED): 未提单 - 新创建的版本，等待提单
- `3` (SUBMITTED): 已提单 - 已提交审批，等待DM审批结果

#### 3.1.3 dss_apiservice_approval（审批单表）
```sql
CREATE TABLE `dss_apiservice_approval` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `api_id` bigint(20) NOT NULL COMMENT '服务api id',
  `api_version_id` bigint(20) NOT NULL COMMENT '版本id',
  `approval_name` varchar(50) NOT NULL COMMENT '审批单名称',
  `apply_user` varchar(1024) NOT NULL COMMENT '申请用户（逗号分隔）',
  `creator` varchar(50) NOT NULL COMMENT '创建者',
  `status` int(10) DEFAULT '0' COMMENT '0待审批，1审批中，2成功，3失败',
  `approval_no` varchar(500) NOT NULL COMMENT '审批单号',
  -- ... 其他字段
  UNIQUE KEY `idx_uniq_api_version_id` (`api_version_id`)
) ENGINE=InnoDB;
```

**字段说明**：
- `apply_user`：用户填写的申请用户，用于 Token 生成
- **增量授权**：系统在生成 Token 时会自动查询历史审批单，合并历史用户

**审批单状态说明**：
- `0` (INITED): 待审批
- `1` (APPROVING): 审批中
- `2` (SUCCESS): 审批通过
- `3` (FAILED): 审批失败

#### 3.1.4 dss_apiservice_token_manager（Token管理表）
```sql
CREATE TABLE `dss_apiservice_token_manager` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `api_version_id` bigint(20) NOT NULL COMMENT '服务api版本id',
  `api_id` bigint(20) NOT NULL COMMENT '服务api配置id',
  `publisher` varchar(20) NOT NULL COMMENT '发布用户',
  `user` varchar(20) NOT NULL COMMENT '申请用户',
  `apply_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '申请时间',
  `duration` int(10) NOT NULL COMMENT '时长',
  `status` tinyint(1) DEFAULT '1' COMMENT '0过期，1有效期内',
  `apply_source` varchar(200) DEFAULT NULL COMMENT '申请来源（审批单号或0001）',
  `token` varchar(500) DEFAULT NULL COMMENT 'token内容',
   PRIMARY KEY (`id`)
) ENGINE=InnoDB;
```

**Token状态说明**：
- `0`: 过期/禁用
- `1`: 有效期

### 3.2 数据流转关系

```
API创建/更新
  ↓
生成新版本（status=2，未提单）
  ↓
用户填写申请用户（只需填写新增用户即可）
  ↓
提交审批（status=3，已提单）
  ↓
DM审批通过
  ↓
用户在页面点击"刷新状态"按钮
  ↓
调用 approvalRefresh 接口
  ↓
请求DM获取审批单状态
  ↓
触发 afterApprovalSuccess() 回调
  ↓
调用 Token 生成逻辑
  ↓
  - 解析申请用户列表
  - 查询历史审批单（getSecondApproval）
  - 查询历史有效 Token（queryByVersionIdWithValidStatus）
  - 合并历史用户 + 申请用户（mergeHistoryToken）
  - 保留历史用户配置（IP 白名单、访问限制等）
  ↓
禁用旧版本 Token（updateTokenStatusBeforeVersionId）
  ↓
批量保存新 Token（saveTokensToDb）
  ↓
版本状态更新为运行中（status=1）
  ↓
所有用户获得新Token
```

## 四、接口设计

### 4.1 核心接口

#### 4.1.1 审批刷新与Token生成接口
```
GET /dss/apiservice/approvalRefresh
```

**请求参数**：
```
approvalNo=xxx  // 审批单号
```

**功能描述**：
- 请求DM获取审批单状态
- 审批通过后自动触发 Token 生成
- **自动合并历史授权用户**（无需用户指定）
- 为所有用户（申请用户 + 历史用户）生成Token
- 禁用旧版本Token

#### 4.1.2 提单接口（保持不变）
```
POST /dss/apiservice/submit
```

**请求参数**：
```json
{
    "submitApiInfos": [
        {"apiId": 1, "apiVersionId": 200}
    ],
    "approvalName": "订单查询API授权",
    "applyUser": "user3,user4",              // 用户只需填写新增用户即可
    "workspaceId": 180,
    "backgroundDesc": "新增授权用户",
    "sensitive": 0,
    "duration": "30"
}
```

**功能说明**：
- 用户只需填写新增的用户
- 系统在 Token 生成时自动合并历史用户
- 无需传递 `incrementalMode` 参数
- 无需传递 `original_apply_user` 参数

**响应**：
```json
{
    "method": "/apiservice/api/submit",
    "status": 0,
    "message": "提单成功"
}
```

## 五、实现方案

### 5.1 增量授权核心实现

#### 5.1.1 实现位置
- **接口**：`ApiServiceTokenRestfulApi.refresh()` - 审批刷新接口
- **核心逻辑**：`DataMapTokenImpl.genTokenRecord()` - Token 生成与合并逻辑

#### 5.1.2 核心流程

**审批刷新与 Token 生成流程**：
```
用户调用 /dss/apiservice/approvalRefresh 接口
  ↓
验证登录用户是否为审批单创建者
  ↓
调用 approvalService.refreshStatus(approvalNo)
  ↓
从 DataMap 获取最新审批状态
  ↓
如果审批状态为"通过"（status=3）：
  ↓
  1. 触发 afterApprovalSuccess 监听器回调
  ↓
  2. 调用 tokenAuth.genTokenRecord(approvalVo)
      ↓
      2.1 解析申请用户列表（逗号分隔）
      ↓
      2.2 为每个用户创建基础 Token 对象
      ↓
      2.3 生成 JWT Token 字符串
      ↓
      2.4 调用 mergeHistoryToken（增量授权核心）
          ↓
          - 获取上一个通过的审批单（getSecondApproval）
          - 查询历史审批中所有有效 Token
          - 找出历史有但本次没有的用户
          - 复制历史配置（IP 白名单等）
          - 合并到当前 Token 列表
  ↓
  3. 调用 tokenAuth.updateTokenStatusBeforeVersionId
      禁用所有旧版本的 Token
  ↓
  4. 调用 tokenAuth.saveTokensToDb 批量保存 Token
  ↓
返回审批状态给前端
```

#### 5.1.3 关键方法实现

**1. getSecondApproval - 获取上一个通过的审批单**

**位置**：`ApprovalServiceImpl.java`

**功能**：根据 API ID 查询上一个通过的审批单，用于增量授权时获取历史用户信息

```java
/**
 * 获取上一个通过的审批单（用于增量授权）
 *
 * 业务场景：
 * 当 API V2 提交审批时，如果需要保留 V1 的授权用户，就需要获取 V1 的审批单信息。
 * 通过查询所有审批通过的记录并排序，返回倒数第二个审批单（最新的前一个）。
 *
 * 使用示例：
 * API V1 审批通过（2026-01-01），授权用户：user1, user2
 * API V2 提交审批（2026-02-01），申请用户：user1, user3
 * 调用此方法获取 V1 的审批单，查询 user1, user2 的 Token 配置
 * 将 user2 的配置复制到 V2，实现增量授权（user1, user2, user3 都有权限）
 *
 * @param apiId API ID
 * @return 上一个通过的审批单，如果不存在返回 null
 */
@Override
public ApprovalVo getSecondApproval(long apiId) {
    // 查询该 API 所有审批通过的审批单（status=3）
    List<ApprovalVo> approvalVoList = queryByApiIdAndStatus(apiId, DataMapStatus.SUCCESS.getIndex());

    // 如果审批通过的记录少于等于 1 条，说明没有上一个审批单（首次提单或只有一次审批）
    if(approvalVoList.size() <= 1){
        return null;
    }

    // 按创建时间升序排序，最新的审批单在列表最后
    approvalVoList.sort(Comparator.comparing(ApprovalVo::getCreateTime));

    // 获取倒数第二个审批单（最新的前一个），即上一次审批通过的记录
    ApprovalVo approvalVo = approvalVoList.get(approvalVoList.size() - 2);
    LOG.info("获取上一个通过的审批单 - API ID: {}, 审批单ID: {}, 审批单号: {}",
             apiId, approvalVo.getId(), approvalVo.getApprovalNo());
    return approvalVoList.get(approvalVoList.size() - 2);
}
```

**2. genTokenRecord - 生成 Token 记录**

**位置**：`DataMapTokenImpl.java`

**功能**：根据审批信息生成 Token 记录，自动合并历史用户

```java
/**
 * 根据审批信息生成 Token 记录
 *
 * 业务流程：
 * 1. 解析审批申请用户列表（逗号分隔字符串，如 "user1,user2,user3"）
 * 2. 为每个用户创建基础 Token 对象（包含 API ID、版本 ID、发布者等基本信息）
 * 3. 生成 JWT Token 字符串（包含用户信息、有效期等）
 * 4. 调用 mergeHistoryToken 合并历史授权用户（增量授权核心）
 * 5. 返回完整的 Token 列表（包含申请用户 + 历史用户）
 *
 * 增量授权说明：
 * - 如果本次申请只包含部分用户（如只申请 user3），系统会自动查询历史版本的用户列表
 * - 将历史用户（如 user1、user2）也包含在 Token 列表中
 * - 保留历史用户的配置信息（IP 白名单、访问限制等）
 *
 * @param approvalVo 审批信息对象
 * @return Token 记录列表（包含所有授权用户：申请用户 + 历史用户）
 */
@Override
public List<TokenManagerVo> genTokenRecord(ApprovalVo approvalVo) {
    List<TokenManagerVo> tokenManagerVoList = new ArrayList<>();

    // 解析申请用户列表（逗号分隔），为每个用户创建 Token
    // 例如：applyUser = "user1,user2,user3"
    Arrays.stream(approvalVo.getApplyUser().split(",")).forEach(tempUser -> {
        TokenManagerVo tmpToken = new TokenManagerVo();
        // 设置 API 基本信息
        tmpToken.setApiId(approvalVo.getApiId());
        tmpToken.setApplyTime(new Date());
        tmpToken.setDuration(approvalVo.getDuration()); // 授权时长（天）
        tmpToken.setReason("approval token auth");
        tmpToken.setStatus(1); // 有效状态
        tmpToken.setIpWhitelist(""); // IP 白名单（默认为空）
        tmpToken.setCaller("scripts");
        tmpToken.setUser(tempUser); // 申请用户
        tmpToken.setPublisher(approvalVo.getCreator()); // 发布者
        tmpToken.setApiVersionId(approvalVo.getApiVersionId()); // API 版本 ID

        // 构造 JWT Token
        ApiServiceToken apiServiceToken = new ApiServiceToken();
        apiServiceToken.setApplyUser(tempUser);
        apiServiceToken.setPublisher(approvalVo.getCreator());
        apiServiceToken.setApplyTime(tmpToken.getApplyTime());
        apiServiceToken.setApiServiceId(approvalVo.getApiId());

        // 生成 JWT Token 字符串（包含用户信息、有效期等）
        tmpToken.setToken(JwtManager.createToken(tempUser, apiServiceToken, tmpToken.getDuration()));
        // 设置审批单号（用于关联审批记录）
        tmpToken.setApplySource(approvalVo.getApprovalNo());
        tokenManagerVoList.add(tmpToken);
    });

    // 合并历史授权用户（增量授权核心逻辑）
    // 会查询上一版本的历史用户，将不在本次申请中的用户也加入到 Token 列表
    mergeHistoryToken(tokenManagerVoList, approvalVo);

    return tokenManagerVoList;
}
```

**3. mergeHistoryToken - 合并历史授权用户**

**位置**：`DataMapTokenImpl.java`

**功能**：将上一版本中已授权但在本次审批申请中未包含的用户，自动合并到当前审批中

```java
/**
 * 合并历史授权用户
 * 将上一版本中已授权但在本次审批申请中未包含的用户，自动合并到当前审批中
 * 增量授权场景：保留历史用户的配置信息（IP 白名单、访问限制等）
 *
 * @param tokenManagerVoList 当前审批申请的用户 token 列表
 * @param approvalVo 当前审批信息
 */
private void mergeHistoryToken(List<TokenManagerVo> tokenManagerVoList, ApprovalVo approvalVo) {
    // 获取上一次审批完成的信息（第二新的审批通过版本）
    ApprovalVo historyApprovalVo = approvalService.getSecondApproval(approvalVo.getApiId());
    if (historyApprovalVo == null) {
        logger.info("api [{},{}] not find history approval",approvalVo.getApiId(),approvalVo.getApprovalName());
        // 没有历史审批记录，无需合并
        return;
    }

    // 提取当前申请的用户列表，用于去重判断
    Set<String> currentUserSet = tokenManagerVoList.stream()
            .map(TokenManagerVo::getUser)
            .collect(Collectors.toSet());

    // 查询上一次审批中所有有效的 token 记录
    List<TokenManagerVo> historyTokenList = apiServiceTokenManagerDao
            .queryByVersionIdWithValidStatus(historyApprovalVo.getApiVersionId(),
                    historyApprovalVo.getApprovalNo(),
                    ApiCommonConstant.API_ENABLE_STATUS);

    logger.info("history token size is {}", historyTokenList.size());
    logger.info("current user is {}",StringUtils.join(currentUserSet,","));
    // 遍历历史 token，找出需要合并的用户（历史中有但本次申请中没有）
    List<TokenManagerVo> tokensToAdd = new ArrayList<>();
    for (TokenManagerVo historyToken : historyTokenList) {
        logger.info("api id is [{},{}], user is {}",historyToken.getApiId(),
                historyToken.getApiVersionId(),historyToken.getUser());
        // 如果该用户已经在当前申请中，跳过
        if (currentUserSet.contains(historyToken.getUser())) {
            continue;
        }

        // 创建新的 token，复制历史 token 的配置信息
        TokenManagerVo newToken = copyHistoryToken(historyToken);
        // 更新为新版本的 API 版本 ID 和审批单号
        newToken.setApiVersionId(approvalVo.getApiVersionId());
        newToken.setApplySource(approvalVo.getApprovalNo());
        newToken.setStatus(1); // 设置为有效状态

        tokensToAdd.add(newToken);
    }

    logger.info("tokensToAdd size is {}", tokensToAdd.size());
    // 将需要合并的 token 添加到列表中（避免在遍历时修改原列表）
    tokenManagerVoList.addAll(tokensToAdd);
}

/**
 * 复制历史 token 的配置信息
 * 保留用户之前设置的 IP 白名单、访问限制等个性化配置
 *
 * @param historyToken 历史 token 配置
 * @return 新的 token 对象，复用历史配置
 */
private TokenManagerVo copyHistoryToken(TokenManagerVo historyToken) {
    TokenManagerVo newToken = new TokenManagerVo();
    // 复制历史 token 的所有属性（除 ID 外）
    BeanUtils.copyProperties(historyToken, newToken);
    newToken.setId(null); // 清空 ID，以便插入新记录
    newToken.setApplyTime(historyToken.getApplyTime() == null ?
            new Date(): historyToken.getApplyTime()); // 如果申请时间空，则更新为当前时间
    return newToken;
}
```

**4. updateTokenStatusBeforeVersionId - 禁用旧版本 Token**

**位置**：`DataMapTokenImpl.java`

**功能**：禁用指定 API 之前版本的所有 Token

```java
/**
 * 禁用指定版本之前的所有 Token
 *
 * 业务场景：
 * 当 API V2 审批通过后，需要禁用 V1 的所有 Token，确保只有最新版本的 Token 有效。
 *
 * 执行逻辑：
 * - 查询该 API（apiId）的所有 Token
 * - 找出 api_version_id 小于当前版本的 Token（即旧版本的 Token）
 * - 将这些 Token 的状态设置为禁用（status=0）
 *
 * 示例：
 * API V1（version_id=100）有 Token：user1, user2
 * API V2（version_id=200）审批通过后，禁用 V1 的所有 Token
 * 之后只有 V2 生成的 Token 有效
 *
 * @param approvalVo 当前审批信息（包含 apiId 和 apiVersionId）
 */
@Override
public void updateTokenStatusBeforeVersionId(ApprovalVo approvalVo){
    apiServiceTokenManagerDao.updateTokenStatusBeforeVersionId(
        approvalVo.getApiVersionId(),    // 当前版本 ID
        approvalVo.getApiId(),           // API ID
        ApiCommonConstant.API_DISABLE_STATUS // 禁用状态
    );
    logger.info("已禁用 API {} 版本 {} 之前的所有 Token", approvalVo.getApiId(), approvalVo.getApiVersionId());
}
```

**5. saveTokensToDb - 批量保存 Token**

**位置**：`DataMapTokenImpl.java`

**功能**：批量保存 Token 到数据库

```java
/**
 * 批量保存 Token 到数据库
 *
 * 业务逻辑：
 * 1. 校验 Token 非空（JWT 字符串）
 * 2. 防止重复授权：检查同一 API 版本 + 审批单号是否已存在 Token
 * 3. 批量插入 Token 记录
 *
 * 重复授权检查说明：
 * - 避免用户多次调用 approvalRefresh 接口导致重复插入 Token
 * - DEFAULT_APPROVAL_NO（"0001"）是特殊审批单号，用于 API 创建者自授权，需要允许重复
 *
 * @param tokenManagerVos Token 列表
 * @param approvalNo 审批单号
 * @return 保存结果枚举
 * @throws ApiServiceTokenException Token 为空或保存失败时抛出
 */
@Override
@Transactional(rollbackFor = ErrorException.class)
public SaveTokenEnum saveTokensToDb(List<TokenManagerVo> tokenManagerVos, String approvalNo) throws ApiServiceTokenException {
    // 校验 Token 非空
    boolean isEmptyToken = tokenManagerVos.stream().anyMatch(tokenManagerVo -> StringUtils.isEmpty(tokenManagerVo.getToken()));
    if (isEmptyToken) {
        throw new ApiServiceTokenException(800001, "Failed to save to db for Some token is empty");
    }
    // 防止重复授权检查
    // 查询该 api_version 和 approvalNo 是否已有 Token 记录（不是默认审批单号）
    // 如果存在记录，说明已经生成过 Token，直接返回成功，避免重复插入
    else if (checkDuplicateAuth(approvalNo, tokenManagerVos.get(0).getApiVersionId()) && !approvalNo.equals(ApiCommonConstant.DEFAULT_APPROVAL_NO)) {
        logger.warn("审批单号 {} 对应的 API 版本 {} 已存在 Token，跳过重复插入",
                   approvalNo, tokenManagerVos.get(0).getApiVersionId());
        return SaveTokenEnum.SUCCESS;
    } else {
        try {
            // 批量插入 Token 记录
            apiServiceTokenManagerDao.insertList(tokenManagerVos);
            logger.info("成功保存 {} 个 Token 到数据库，审批单号: {}", tokenManagerVos.size(), approvalNo);
        } catch (Exception e) {
            logger.error("批量保存 Token 到数据库失败", e);
            throw new ApiServiceTokenException(800002, e.getMessage());
        }
    }
    return SaveTokenEnum.SUCCESS;
}

private boolean checkDuplicateAuth(String approvalNo, Long apiServiceVersionID) {
    if (apiServiceTokenManagerDao.queryApprovalNo(approvalNo, apiServiceVersionID) > 0) {
        return true;
    } else {
        return false;
    }
}
```

## 六、接口使用场景

### 6.1 场景一：首次提单（无历史用户）

**步骤**：
1. 用户创建API
2. 调用 `GET /dss/apiservice/availableSubmitApi` 查询可提单API
3. 用户填写提单信息：
   ```json
   {
       "submitApiInfos": [{"apiId": 1, "apiVersionId": 100}],
       "approvalName": "订单查询API首次授权",
       "applyUser": "user1,user2",
       "duration": "30"
   }
   ```
4. 调用 `POST /dss/apiservice/submit` 提单
5. DM审批通过后，用户在页面点击"刷新状态"按钮
6. 调用 `GET /dss/apiservice/approvalRefresh?approvalNo=xxx` 接口
7. 系统自动为user1和user2生成Token

**结果**：
- user1、user2 获得访问权限
- Token 状态为有效（status=1）

### 6.2 场景二：增量提单（自动合并历史用户）

**步骤**：
1. API已运行，user1和user2已有Token（V1 版本）
2. 用户需要新增user3和user4
3. 用户创建新版本（V2）
4. **用户只需填写新增的用户**：
   ```json
   {
       "submitApiInfos": [{"apiId": 1, "apiVersionId": 200}],
       "approvalName": "订单查询API增量授权",
       "applyUser": "user3,user4",  // 只需填写新增用户
       "duration": "30"
   }
   ```
5. 调用 `POST /dss/apiservice/submit` 提单
6. DM审批通过后，用户在页面点击"刷新状态"按钮
7. 调用 `GET /dss/apiservice/approvalRefresh?approvalNo=xxx` 接口
8. **系统自动执行增量授权逻辑**：
   - 查询上一个通过的审批单（V1）
   - 获取V1的有效Token（user1, user2）
   - 对比V2申请用户列表（user3, user4）
   - 自动合并：user1, user2, user3, user4
9. 系统为所有用户生成Token

**结果**：
- user1、user2（历史用户）获得新的Token
- user3、user4（新增用户）获得Token
- V1版本的Token被禁用
- V2版本的Token生效

**核心优势**：
- 用户无需填写历史用户（user1, user2），只需填写新增用户（user3, user4）
- 系统自动合并历史用户和新增用户
- 保留历史用户的配置信息（IP 白名单、访问限制等）

### 6.3 场景三：只保留历史用户

**步骤**：
1. API已运行，user1和user2已有Token（V1 版本）
2. 用户创建新版本（V2），但不新增任何用户
3. **用户申请用户为空**：
   ```json
   {
       "submitApiInfos": [{"apiId": 1, "apiVersionId": 200}],
       "approvalName": "订单查询API版本更新",
       "applyUser": "",  // 空字符串
       "duration": "30"
   }
   ```
4. 调用 `POST /dss/apiservice/submit` 提单
5. DM审批通过后，用户在页面点击"刷新状态"按钮
6. 调用 `GET /dss/apiservice/approvalRefresh?approvalNo=xxx` 接口
7. **系统自动合并历史用户**：
   - 查询上一个通过的审批单（V1）
   - 获取V1的有效Token（user1, user2）
   - 自动保留：user1, user2
8. 系统为用户生成Token

**结果**：
- user1、user2 获得新的Token（历史用户保留）
- V1版本的Token被禁用
- V2版本的Token生效

**核心优势**：
- 用户无需填写任何用户
- 系统自动保留所有历史用户
- 适用于API版本升级但授权用户不变的场景

## 七、测试计划

### 7.1 单元测试
- 增量提单逻辑测试
- Token生成逻辑测试
- 合并历史用户逻辑测试

### 7.2 集成测试
- 首次提单流程测试
- 增量提单流程测试
- approvalRefresh 接口生成Token测试

### 7.3 边界条件测试
- 无历史用户的提单
- 空申请用户的提单（只保留历史用户）
- 历史用户与申请用户重叠
- 多API批量提单
- 重复生成Token测试

### 7.4 性能测试
- 大量用户Token生成性能测试
- 批量API提单性能测试

## 八、风险评估

### 8.1 技术风险
1. **用户合并逻辑复杂度**：多个API批量提单时，用户合并逻辑需要仔细处理
    - 缓解措施：充分测试，确保逻辑正确

2. **Token生成性能**：大量用户时可能影响性能
    - 缓解措施：批量插入、异步处理

3. **回调失败不影响主流程**：Token生成失败不应影响审批流程
    - 缓解措施：捕获异常，记录日志

### 8.2 业务风险
1. **权限控制**：增量授权需要严格控制权限
    - 缓解措施：仅API创建者可调用 approvalRefresh 接口

2. **数据一致性**：Token和审批单数据需要保持一致
    - 缓解措施：事务控制、数据校验

## 九、验收标准

1. ✅ 审批通过后，系统自动合并历史授权用户
2. ✅ 正确保留历史用户的配置信息（IP 白名单、访问限制等）
3. ✅ approvalRefresh 接口生成Token功能正常
4. ✅ 支持批量API提单
5. ✅ 数据一致性保证
6. ✅ 性能满足要求
7. ✅ 边界条件处理正确
8. ✅ 禁用旧版本Token机制正常

## 十一、附录

### 11.1 相关文件
- `ApiServiceCoreRestfulApi.java` - REST接口定义
- `ApiServiceImpl.java` - 业务逻辑实现
- `ApprovalServiceImpl.java` - 审批服务实现
- `ApiServiceVersionDao.java` - API版本DAO
- `ApiServiceApiVersionMapper.xml` - API版本Mapper XML
- `ApiServiceTokenManagerDao.java` - Token管理DAO
- `ApiServiceTokenManagerMapper.xml` - Token管理Mapper XML
- `ApiSubmitVo.java` - 提单请求VO

### 11.2 数据库变更

#### 11.2.1 新增查询方法

无需新增表或字段，仅在现有表中查询数据。

**ApiServiceApprovalDao.java**
```java
/**
 * 根据 API ID 和状态查询审批记录列表
 * 用于增量授权时获取上一个通过的审批单
 */
List<ApprovalVo> queryByApiIdAndStatus(@Param("apiId") Long apiId,
                                       @Param("status") Integer status);
```

**ApiServiceApprovalMapper.xml**
```xml
<!--
    根据 API ID 和状态查询审批记录列表

    业务场景（获取上一个通过的审批单）：
    1. API 有多个版本，每个版本对应一个审批单
    2. 当需要实现增量授权时，需要获取上一个通过的审批单
    3. 调用此查询: SELECT * FROM approval WHERE api_id=1 AND status=3 ORDER BY create_time DESC
    4. 返回结果按创建时间降序排列，最新的审批单在第一位
    5. 取第二个元素（索引 1），即上一个通过的审批单
-->
<select id="queryByApiIdAndStatus" resultMap="approvalMap">
    SELECT id, api_id, api_version_id, approval_name, apply_user,
           creator, status, approval_no, create_time, update_time
    FROM dss_apiservice_approval
    WHERE api_id = #{apiId}
      AND status = #{status}
    ORDER BY create_time DESC
</select>
```

**ApiServiceTokenManagerDao.java**
```java
/**
 * 查询某个API版本的所有有效Token（用于增量授权）
 */
List<TokenManagerVo> queryByVersionIdWithValidStatus(@Param("apiVersionId") Long apiVersionId,
                                                   @Param("approvalNo") String approvalNo,
                                                   @Param("status") Integer status);
```

**ApiServiceTokenManagerMapper.xml**
```xml
<!--
    查询某个 API 版本的所有有效Token（用于增量授权）

    业务场景（增量授权）：
    1. API V1 审批通过，审批单号 "uuid-v1"，生成 Token: user1, user2
    2. API V2 提交审批，审批单号 "uuid-v2"
    3. 调用此查询获取 V1 的 Token: WHERE api_version_id=100 AND apply_source='uuid-v1' AND status=1
    4. 对比 V2 申请用户列表，发现 user2 不在 V2 申请中
    5. 将 user2 的配置复制到 V2，实现增量授权
-->
<select id="queryByVersionIdWithValidStatus" resultMap="tokenManagerMap">
    SELECT
        id, api_version_id, api_id, publisher, user, apply_time,
        duration, reason, ip_whitelist, status, caller, access_limit,
        token, apply_source
    FROM dss_apiservice_token_manager
    WHERE api_version_id = #{apiVersionId}
      AND apply_source = #{approvalNo}
      AND status = #{status}
    ORDER BY apply_time DESC
</select>
```

**ApiServiceTokenManagerDao.java**
```java
/**
 * 禁用指定 API 之前版本的所有 Token
 */
void updateTokenStatusBeforeVersionId(@Param("apiId") Long apiId,
                                     @Param("apiVersionId") Long apiVersionId,
                                     @Param("status") Integer status);
```

**ApiServiceTokenManagerMapper.xml**
```xml
<!--
    禁用指定 API 之前版本的所有 Token
    用途：当 API 新版本审批通过后，禁用旧版本的所有 Token，确保只有最新版本的 Token 有效

    业务场景：
    API V1 (version_id=100) 有 Token: user1, user2
    API V2 (version_id=200) 审批通过后，执行此 SQL
    将所有 api_version_id < 200 的 Token 状态设置为 0（禁用）
    之后只有 V2 生成的 Token 有效
-->
<update id="updateTokenStatusBeforeVersionId">
    UPDATE dss_apiservice_token_manager
    SET status = #{status}
    WHERE api_id = #{apiId}
      AND api_version_id < #{apiVersionId}
</update>
```

**查询流程说明**：
1. 调用 `approvalService.getSecondApproval(apiId)` 获取上一个通过的审批单
2. 使用审批单的 `apiVersionId`、`approvalNo` 调用 `queryByVersionIdWithValidStatus(versionId, approvalNo, status)` 查询该版本的有效Token用户
3. 对比当前申请用户列表，自动合并历史用户

### 11.3 相关常量
```java
// ApiCommonConstant
public static final String DEFAULT_APPROVAL_NO = "0001";
public static final Integer API_DISABLE_STATUS = 0;
public static final Integer API_ENABLE_STATUS = 1;
```

### 11.4 状态枚举
```java
// ApiVersionStatusEnum
NOT_SUBMITTED(2, "未提单"),
SUBMITTED(3, "已提单"),
ENABLE(1, "运行中"),
DISABLE(0, "禁用");

// DataMapStatus
INITED(0, "待审批"),
APPROVING(1, "审批中"),
SUCCESS(2, "成功"),
FAILED(3, "失败");
```

---

**文档版本**：v7
**创建日期**：2026-02-27
**最后更新**：2026-03-04
**文档状态**：已完成

---

## 十二、核心原则说明

### 12.1 增量授权的核心逻辑

**关键原则**：
1. **版本隔离**：每个API版本有独立的Token集合
2. **手动刷新生成Token**：审批通过后，用户在页面点击"刷新状态"按钮，调用 `approvalRefresh` 接口生成Token
3. **自动合并**：系统自动查询历史审批单，合并历史用户 + 申请用户（去重）
4. **保留历史配置**：保留历史用户的配置信息（IP 白名单、访问限制等）

### 12.2 Token生成的业务逻辑

**场景对比**：

| 场景 | applyUser填写 | 历史user1 | 历史user2 | 新增user3 | 结果 |
|------|-------------|----------|----------|----------|------|
| 首次提单 | user1,user2 | - | - | 生成Token | user1,user2获得授权 |
| 增量提单 | user3 | 自动合并 | 自动合并 | 生成Token | 所有用户获得授权 |
| 空用户提单 | 空 | 自动合并 | 自动合并 | - | 保留历史用户 |

**重要说明**：
- 用户只需填写新增的用户，历史用户由系统自动合并
- 不区分历史用户和新增用户，统一生成Token
- 所有用户都获得新Token，旧Token被禁用
- 保留历史用户的配置信息（IP 白名单、访问限制等）

---

## 十三、更新记录

### v7 (2026-03-04)
**文档重构**：

#### 13.1 主要变更

**1. 取消的功能**：
- ❌ 删除"查询API历史授权用户"接口（GET /dss/apiservice/history/users）
- ❌ 删除 `incrementalMode` 参数（在提单接口中）
- ❌ 删除 `original_apply_user` 字段（在 approval 表中）

**2. 核心调整**：
- ✅ 增量授权改为**自动执行**，无需用户指定任何参数
- ✅ 系统在 Token 生成时自动查询历史审批单，合并历史用户
- ✅ 用户只需填写新增的用户，系统自动合并历史用户
- ✅ 保留历史用户的配置信息（IP 白名单、访问限制等）

**3. 文档结构优化**：
- 简化功能概述，突出自动增量授权的核心特性
- 更新数据流转关系，描述完整的自动增量授权流程
- 简化接口设计，删除不必要的参数说明
- 更新实现方案，详细描述核心方法的实现逻辑
- 优化使用场景，突出用户只需填写新增用户的优势
- 更新数据库变更，删除不需要的字段
- 调整验收标准，取消相关条目

#### 13.2 增量授权工作原理

**核心流程**：
```
用户提单时：
  - 只需填写新增的用户
  - 无需填写历史用户
  - 无需指定 incrementalMode 参数

审批通过后：
  - 用户点击"刷新状态"按钮
  - 调用 approvalRefresh 接口
  - 系统自动执行：
    1. 查询上一个通过的审批单（getSecondApproval）
    2. 获取历史审批中所有有效 Token
    3. 对比当前申请用户列表
    4. 自动合并历史用户 + 申请用户
    5. 保留历史用户的配置信息
    6. 禁用旧版本 Token
    7. 生成新版本 Token
```

**关键优势**：
- 用户体验好：无需记住历史用户，只需填写新增用户
- 系统自动化：系统自动处理所有增量授权逻辑
- 配置保留：保留历史用户的个性化配置
- 版本隔离：每个版本有独立的 Token 集合
- 数据一致：禁用旧版本 Token，确保只有最新版本生效

### v6 (2026-03-04)
**代码分析与注释完善**：

#### 13.1 Commit b3721c877 分析

**Commit 信息**：`新增数据服务审批与增量授权功能 #AI commit#`

**修改文件列表**：
1. `ApiServiceApprovalDao.java` - 新增 `queryByApiIdAndStatus` 方法
2. `ApiServiceTokenManagerDao.java` - 新增 `queryByVersionIdWithValidStatus` 方法
3. `ApiServiceApprovalMapper.xml` - 新增 `queryByApiIdAndStatus` SQL 映射
4. `ApiServiceTokenManagerMapper.xml` - 新增 `queryByApprovalNo` SQL 映射
5. `ApiServiceTokenRestfulApi.java` - 增强 `refresh` 接口逻辑
6. `ApprovalService.java` - 新增 `getSecondApproval` 接口方法
7. `ApiServiceImpl.java` - 修改 `submitToDm` 方法
8. `ApprovalServiceImpl.java` - 实现 `getSecondApproval` 方法
9. `DataMapTokenImpl.java` - 实现增量授权核心逻辑
10. `TokenAuth.java` - 新增 `updateTokenStatusBeforeVersionId` 接口方法

#### 13.2 核心功能实现

**审批刷新与 Token 生成流程**：
```
用户调用 /dss/apiservice/approvalRefresh 接口
  ↓
验证登录用户是否为审批单创建者
  ↓
调用 approvalService.refreshStatus(approvalNo)
  ↓
从 DataMap 获取最新审批状态
  ↓
如果审批状态为"通过"（status=3）：
  ↓
  1. 触发 afterApprovalSuccess 监听器回调
  ↓
  2. 调用 tokenAuth.genTokenRecord(approvalVo)
      ↓
      2.1 解析申请用户列表（逗号分隔）
      ↓
      2.2 为每个用户创建基础 Token
      ↓
      2.3 调用 mergeHistoryToken（增量授权核心）
          ↓
          - 获取上一个通过的审批单（getSecondApproval）
          - 查询历史审批中所有有效 Token
          - 找出历史有但本次没有的用户
          - 复制历史配置（IP 白名单等）
          - 合并到当前 Token 列表
  ↓
  3. 调用 tokenAuth.updateTokenStatusBeforeVersionId
      禁用所有旧版本的 Token
  ↓
  4. 调用 tokenAuth.saveTokensToDb 批量保存 Token
  ↓
返回审批状态给前端
```

#### 13.3 增量授权功能详解

**业务场景**：
- API V1 审批通过（2026-01-01），授权用户：user1, user2
- API V2 提交审批（2026-02-01），申请用户：user1, user3
- **期望结果**：V2 审批通过后，user1、user2、user3 都有访问权限

**实现方式**：
1. 通过 `getSecondApproval` 获取 V1 的审批单
2. 查询 V1 的所有有效 Token（user1, user2）
3. 对比当前申请用户列表（user1, user3）
4. 找出差异：user2 是历史用户但在本次申请中未包含
5. 复制 user2 的 Token 配置（IP 白名单、访问限制等）
6. 将 user2 的 Token 也添加到 V2 的授权列表中

**保留的配置信息**：
- IP 白名单（ip_whitelist）
- 访问限制（access_limit）
- 调用方（caller）
- 其他个性化配置

#### 13.4 代码注释完善

**ApprovalServiceImpl.java** - `getSecondApproval` 方法：
```java
/**
 * 获取上一个通过的审批单（用于增量授权）
 * 
 * 业务场景：
 * 当 API V2 提交审批时，如果需要保留 V1 的授权用户，就需要获取 V1 的审批单信息。
 * 通过查询所有审批通过的记录并排序，返回倒数第二个审批单（最新的前一个）。
 * 
 * 使用示例：
 * API V1 审批通过（2026-01-01），授权用户：user1, user2
 * API V2 提交审批（2026-02-01），申请用户：user1, user3
 * 调用此方法获取 V1 的审批单，查询 user1, user2 的 Token 配置
 * 将 user2 的配置复制到 V2，实现增量授权（user1, user2, user3 都有权限）
 * 
 * @param apiId API ID
 * @return 上一个通过的审批单，如果不存在返回 null
 */
```

**DataMapTokenImpl.java** - `saveTokensToDb` 方法：
```java
/**
 * 批量保存 Token 到数据库
 * 
 * 业务逻辑：
 * 1. 校验 Token 非空（JWT 字符串）
 * 2. 防止重复授权：检查同一 API 版本 + 审批单号是否已存在 Token
 * 3. 批量插入 Token 记录
 * 
 * 重复授权检查说明：
 * - 避免用户多次调用 approvalRefresh 接口导致重复插入 Token
 * - DEFAULT_APPROVAL_NO（"0001"）是特殊审批单号，用于 API 创建者自授权，需要允许重复
 */
```

**DataMapTokenImpl.java** - `genTokenRecord` 方法：
```java
/**
 * 根据审批信息生成 Token 记录
 * 
 * 业务流程：
 * 1. 解析审批申请用户列表（逗号分隔字符串）
 * 2. 为每个用户创建基础 Token 对象
 * 3. 生成 JWT Token 字符串
 * 4. 调用 mergeHistoryToken 合并历史授权用户（增量授权核心）
 * 5. 返回完整的 Token 列表（包含申请用户 + 历史用户）
 * 
 * 增量授权说明：
 * - 如果本次申请只包含部分用户，系统会自动查询历史版本的用户列表
 * - 将历史用户也包含在 Token 列表中
 * - 保留历史用户的配置信息（IP 白名单、访问限制等）
 */
```

**DataMapTokenImpl.java** - `mergeHistoryToken` 方法：
```java
/**
 * 合并历史授权用户
 * 将上一版本中已授权但在本次审批申请中未包含的用户，自动合并到当前审批中
 * 增量授权场景：保留历史用户的配置信息（IP 白名单、访问限制等）
 * 
 * @param tokenManagerVoList 当前审批申请的用户 token 列表
 * @param approvalVo 当前审批信息
 */
```

**DataMapTokenImpl.java** - `copyHistoryToken` 方法：
```java
/**
 * 复制历史 token 的配置信息
 * 保留用户之前设置的 IP 白名单、访问限制等个性化配置
 * 
 * @param historyToken 历史 token 配置
 * @return 新的 token 对象，复用历史配置
 */
```

**DataMapTokenImpl.java** - `updateTokenStatusBeforeVersionId` 方法：
```java
/**
 * 禁用指定版本之前的所有 Token
 * 
 * 业务场景：
 * 当 API V2 审批通过后，需要禁用 V1 的所有 Token，确保只有最新版本的 Token 有效。
 * 
 * 执行逻辑：
 * - 查询该 API（apiId）的所有 Token
 * - 找出 api_version_id 小于当前版本的 Token（即旧版本的 Token）
 * - 将这些 Token 的状态设置为禁用（status=0）
 */
```

#### 13.5 Mapper XML 注释完善

**ApiServiceApprovalMapper.xml** - `queryByApiIdAndStatus`：
```xml
<!--
    根据 API ID 和状态查询审批记录列表

    业务场景（获取上一个通过的审批单）：
    1. API 有多个版本，每个版本对应一个审批单
    2. 当需要实现增量授权时，需要获取上一个通过的审批单
    3. 调用此查询: SELECT * FROM approval WHERE api_id=1 AND status=3 ORDER BY create_time DESC
    4. 返回结果按创建时间降序排列，最新的审批单在第一位
    5. 取第二个元素（索引 1），即上一个通过的审批单
-->
```

**ApiServiceTokenManagerMapper.xml** - `updateTokenStatusBeforeVersionId`：
```xml
<!--
    禁用指定 API 之前版本的所有 Token
    用途：当 API 新版本审批通过后，禁用旧版本的所有 Token，确保只有最新版本的 Token 有效

    业务场景：
    API V1 (version_id=100) 有 Token: user1, user2
    API V2 (version_id=200) 审批通过后，执行此 SQL
    将所有 api_version_id < 200 的 Token 状态设置为 0（禁用）
    之后只有 V2 生成的 Token 有效
-->
```

**ApiServiceTokenManagerMapper.xml** - `queryByVersionIdWithValidStatus`：
```xml
<!--
    查询某个 API 版本的所有有效 Token（用于增量授权）

    业务场景（增量授权）：
    1. API V1 审批通过，审批单号 "uuid-v1"，生成 Token: user1, user2
    2. API V2 提交审批，审批单号 "uuid-v2"
    3. 调用此查询获取 V1 的 Token: WHERE api_version_id=100 AND apply_source='uuid-v1' AND status=1
    4. 对比 V2 申请用户列表，发现 user2 不在 V2 申请中
    5. 将 user2 的配置复制到 V2，实现增量授权
-->
```

#### 13.6 关键技术点

**1. 防重复授权机制**：
- 在 `saveTokensToDb` 方法中检查 `approvalNo` 和 `apiVersionId` 是否已存在 Token
- 避免用户多次调用 `approvalRefresh` 接口导致重复插入 Token
- 特殊处理 `DEFAULT_APPROVAL_NO`（"0001"），允许重复（用于 API 创建者自授权）

**2. 增量授权核心逻辑**：
- 通过 `getSecondApproval` 获取上一个通过的审批单
- 查询历史审批中所有有效 Token
- 找出历史有但本次没有的用户
- 复制历史配置（IP 白名单、访问限制等）
- 将这些用户添加到当前 Token 列表

**3. Token 状态管理**：
- `updateTokenStatusBeforeVersionId`：禁用旧版本 Token
- 确保只有最新版本的 Token 有效
- 通过 `api_version_id` 对比判断新旧版本

**4. 监听器模式**：
- 审批状态变更时触发监听器回调
- `afterApprovalSuccess`：审批成功回调
- `afterApprovalFailed`：审批失败回调
- 支持注册多个监听器

#### 13.7 测试建议

**增量授权测试场景**：
1. **首次提单**：无历史用户，申请 user1, user2
2. **增量提单**：申请 user1, user3，自动合并 user2
3. **重复用户**：申请 user1, user2（历史已有），不重复添加
4. **无新增用户**：申请空用户列表，自动保留所有历史用户
5. **多 API 批量**：批量提单多个 API，每个 API 独立处理增量授权

**边界条件测试**：
- 无历史审批记录（首次提单）
- 历史审批记录少于 2 条
- 申请用户列表为空
- 申请用户列表包含重复用户
- 历史用户全部在申请列表中

---

### v5 (2026-03-02)
**修复内容**：
- ApiServiceImpl 中已存在 `getMaxApprovedVersion()` 方法（第1061行）
- 在 ApiService 接口中添加 `getMaxApprovedVersion()` 方法定义
- 将 ApiServiceImpl 中的两处 Stream 查询改为调用 `getMaxApprovedVersion()` 方法
- 发现 ApiServiceImpl 中已有 `getMaxVersion()` 和 `getSecondMaxVersion()` 方法（第1038行、1045行）
- 在 ApiService 接口中添加 `getMaxVersion()` 和 `getSecondMaxVersion()` 方法定义
- 修复接口与实现不一致的问题，所有方法均有 `@Override` 注解
- 无需新增任何函数，直接复用现有实现
- 所有文件通过编译检查

### v4 (2026-03-02)
**优化内容**：
- 删除 DAO 层的 `getMaxApprovedVersion()` 方法
- 删除 Service 接口的 `getMaxApprovedVersion()` 方法
- 使用现有的 `queryApiVersionByApiServiceId()` 方法，通过 Stream 过滤获取运行中版本
- 修复 ApiServiceImpl 中的方法调用，直接使用 Stream 查询
- 简化代码，避免创建冗余的 DAO 方法
- 更新文档中的所有相关代码示例

### v2 (2026-03-02)
**修复内容**：
- 修复DAO层方法引用错误：`queryApprovedVersionByApiId()` → `getMaxApprovedVersion()`
- 在 `ApiServiceVersionDao.java` 中新增 `getMaxApprovedVersion()` 方法声明
- 在 `ApiServiceApiVersionMapper.xml` 中新增 `getMaxApprovedVersion` SQL查询映射
- 修复Service层 `queryApiHistoryUsers()` 方法中的方法调用
- 修复Service层 `mergeIncrementalUsers()` 方法中的方法调用
- 更新文档中所有相关的代码示例和实现说明
- 删除未使用的函数：`getMaxVersion()`、`getSecondMaxVersion()`、`getMaxSubmittedVersion()`
- 仅保留 `getMaxApprovedVersion()` 函数（增量授权功能使用）

### v1 (2026-02-27)
**主要变更**：
- 增量模式下，总是自动合并历史用户 + 新增用户
- submit 接口添加incrementalMode(是否增量授权模式)参数
- Token生成功能使用已有的 `approvalRefresh` 接口（已实现）
- 用户在页面点击"刷新状态"按钮，调用 `approvalRefresh` 接口生成Token
- `afterApprovalSuccess()` 回调保持原有逻辑，不生成Token
- 保留用户填写的原始授权用户信息，在 `dss_apiservice_approval` 表新增 `original_apply_user` 字段
