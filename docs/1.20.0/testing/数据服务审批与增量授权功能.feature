# language: zh-CN
功能: 数据服务审批与增量授权功能
  作为API管理员
  我希望审批通过后能自动合并历史授权用户
  以便简化增量授权流程并保留历史配置

  背景:
    假如 系统已启动且数据库连接正常
    而且 用户已登录系统
    而且 API服务模块正常运行

  @smoke @critical @E1
  场景: 批量提单 - 正常提单多个API
    假如 数据库中存在以下未提单的API:
      | API ID | API名称 | 版本ID | 版本状态 |
      | 100    | 订单查询API | 1001 | 未提单 |
      | 101    | 用户管理API | 1002 | 未提单 |
    当 用户批量提单并填写以下信息:
      | 审批单名称 | 授权用户 | 授权期限 | 工作空间ID |
      | API批量授权申请 | user1,user2 | 30 | 180 |
    那么 系统应该返回提单成功
    而且 每个API版本应该创建一条审批记录
    而且 API版本状态应该更新为"已提单"
    而且 系统应该生成统一审批单号

  @smoke @critical @E1
  场景: 批量提单 - 支持永久授权
    假如 数据库中存在未提单的API(ID=100, 版本ID=1001)
    当 用户提单并设置授权期限为永久(duration="*")
    那么 系统应该接受永久授权申请
    而且 审批记录中的授权期限应该为永久

  @smoke @critical @E1
  场景: 批量提单 - JDBC API特殊处理
    假如 数据库中存在未提单的JDBC类型API(ID=100, 版本ID=1001)
    而且 该API配置了开发负责人和产品信息
    当 用户提单该API
    那么 系统应该包含开发负责人信息
    而且 系统应该包含产品信息

  @smoke @critical @E2
  场景: 审批刷新 - 审批通过后自动生成Token
    假如 数据库中存在审批单(审批单号="uuid-001")
    而且 审批单状态为"审批中"
    而且 DM系统返回审批状态为"通过"
    而且 当前登录用户为审批单创建者
    当 用户调用审批刷新接口 approvalRefresh
    那么 系统应该更新审批单状态为"成功"
    而且 系统应该自动为所有申请用户生成Token
    而且 系统应该禁用旧版本Token

  @smoke @critical @E2
  场景: 审批刷新 - 仅允许创建者调用
    假如 数据库中存在审批单(审批单号="uuid-001", 创建者="user_admin")
    而且 当前登录用户为"user_other"
    当 用户调用审批刷新接口 approvalRefresh
    那么 系统应该拒绝访问
    而且 系统应该返回权限错误提示"只有审批单创建者可以刷新状态"

  @smoke @critical @E3
  场景: 增量授权 - 首次提单无历史用户
    假如 数据库中存在API(ID=100, 版本ID=1001)
    而且 该API没有历史审批记录
    当 用户首次提单并填写授权用户"user1,user2"
    而且 审批通过后调用审批刷新接口
    那么 系统应该为user1生成Token
    而且 系统应该为user2生成Token
    而且 Token状态应该为有效
    而且 不应该合并任何历史用户

  @smoke @critical @E3
  场景: 增量授权 - 自动合并历史用户
    假如 数据库中存在以下历史审批记录:
      | API ID | 版本ID | 审批单号 | 授权用户 | 状态 |
      | 100    | 1001   | uuid-v1  | user1,user2 | 成功 |
    而且 存在历史Token记录:
      | 用户 | 版本ID | 审批单号 | IP白名单 | 状态 |
      | user1 | 1001 | uuid-v1 | 192.168.1.100 | 有效 |
      | user2 | 1001 | uuid-v1 | 192.168.1.101 | 有效 |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并只填写新增用户"user3,user4"
    而且 审批通过后调用审批刷新接口
    那么 系统应该自动查询历史审批单
    而且 系统应该自动合并历史用户user1和user2
    而且 系统应该为user1生成新版本Token
    而且 系统应该为user2生成新版本Token
    而且 系统应该为user3生成Token
    而且 系统应该为user4生成Token
    而且 历史版本(1001)的Token应该被禁用
    而且 新版本(1002)的Token应该有效

  @smoke @critical @E3
  场景: 增量授权 - 只保留历史用户
    假如 数据库中存在历史审批记录:
      | API ID | 版本ID | 审批单号 | 授权用户 | 状态 |
      | 100    | 1001   | uuid-v1  | user1,user2 | 成功 |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并填写授权用户为空
    而且 审批通过后调用审批刷新接口
    那么 系统应该自动保留历史用户user1和user2
    而且 系统应该为user1生成新版本Token
    而且 系统应该为user2生成新版本Token
    而且 不应该生成任何新增用户的Token

  @smoke @critical @E3
  场景: 增量授权 - 重复用户去重处理
    假如 数据库中存在历史审批记录:
      | API ID | 版本ID | 审批单号 | 授权用户 | 状态 |
      | 100    | 1001   | uuid-v1  | user1,user2 | 成功 |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并填写授权用户"user1,user3"
    而且 审批通过后调用审批刷新接口
    那么 系统应该自动合并历史用户user2
    而且 系统应该为user1生成Token(去重后只生成一次)
    而且 系统应该为user2生成Token
    而且 系统应该为user3生成Token
    而且 最终授权用户应该为user1,user2,user3

  @smoke @critical @E4
  场景: 历史配置保留 - 保留IP白名单配置
    假如 数据库中存在历史Token记录:
      | 用户 | 版本ID | IP白名单 | 访问限制 | caller |
      | user1 | 1001 | 192.168.1.100,192.168.1.101 | 1000次/天 | scriptis |
      | user2 | 1001 | 192.168.1.200 | 500次/天 | workflow |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并只填写新增用户"user3"
    而且 审批通过后调用审批刷新接口
    那么 系统应该保留user1的IP白名单配置"192.168.1.100,192.168.1.101"
    而且 系统应该保留user1的访问限制"1000次/天"
    而且 系统应该保留user1的caller"scriptis"
    而且 系统应该保留user2的IP白名单配置"192.168.1.200"
    而且 系统应该保留user2的访问限制"500次/天"
    而且 系统应该保留user2的caller"workflow"
    而且 user3应该使用默认配置(空IP白名单,无访问限制)

  @regression @boundary
  场景: 边界条件 - 无历史审批记录
    假如 数据库中存在API(ID=100, 版本ID=1001)
    而且 该API没有历史审批记录
    当 用户首次提单
    而且 审批通过后调用审批刷新接口
    那么 系统应该正常处理
    而且 不应该尝试查询历史用户
    而且 只为申请用户生成Token

  @regression @boundary
  场景: 边界条件 - 历史审批记录少于2条
    假如 数据库中存在API(ID=100)
    而且 该API只有1条审批通过的记录
    当 用户创建新版本并提单
    而且 审批通过后调用审批刷新接口
    那么 系统应该判断为首次提单或只有一次审批
    而且 getSecondApproval方法应该返回null
    而且 不应该合并历史用户

  @regression @boundary
  场景: 边界条件 - 申请用户列表为空
    假如 数据库中存在历史审批记录:
      | API ID | 版本ID | 授权用户 | 状态 |
      | 100    | 1001   | user1,user2 | 成功 |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并填写授权用户为空字符串""
    而且 审批通过后调用审批刷新接口
    那么 系统应该解析申请用户列表为空
    而且 系统应该自动保留所有历史用户
    而且 最终只为历史用户user1和user2生成Token

  @regression @boundary
  场景: 边界条件 - 申请用户全部在历史用户中
    假如 数据库中存在历史审批记录:
      | API ID | 版本ID | 授权用户 | 状态 |
      | 100    | 1001   | user1,user2,user3 | 成功 |
    而且 用户创建新版本(V2, 版本ID=1002)
    当 用户提单并填写授权用户"user1,user2"
    而且 审批通过后调用审批刷新接口
    那么 系统应该自动合并历史用户user3
    而且 最终授权用户应该为user1,user2,user3
    而且 不应该重复生成user1和user2的Token

  @regression @boundary
  场景: 边界条件 - 重复调用approvalRefresh接口
    假如 数据库中存在审批单(审批单号="uuid-001")
    而且 审批单状态为"成功"
    而且 该审批单已生成Token记录
    当 用户再次调用审批刷新接口 approvalRefresh
    那么 系统应该检测到已存在Token
    而且 系统应该跳过重复插入
    而且 系统应该返回成功状态
    而且 数据库中不应该有重复的Token记录

  @regression @integration
  场景: 多API批量提单 - 独立处理每个API的增量授权
    假如 数据库中存在以下API及其历史记录:
      | API ID | API名称 | 当前版本ID | 历史版本ID | 历史授权用户 |
      | 100    | 订单查询API | 1002 | 1001 | user1,user2 |
      | 101    | 用户管理API | 2002 | 2001 | user3,user4 |
    当 用户批量提单并填写以下信息:
      | API ID列表 | 授权用户 | 审批单名称 |
      | 100,101 | user5,user6 | 多API批量授权 |
    而且 审批通过后调用审批刷新接口
    那么 系统应该独立处理每个API的增量授权
    而且 订单查询API的最终授权用户应该为user1,user2,user5,user6
    而且 用户管理API的最终授权用户应该为user3,user4,user5,user6
    而且 每个API的历史版本Token应该被禁用

  @regression @integration
  场景: 禁用旧版本Token机制
    假如 数据库中存在以下Token记录:
      | 用户 | API ID | 版本ID | 状态 | 审批单号 |
      | user1 | 100 | 1001 | 有效 | uuid-v1 |
      | user2 | 100 | 1001 | 有效 | uuid-v1 |
    而且 用户创建新版本(V2, 版本ID=1002)并审批通过
    当 系统调用updateTokenStatusBeforeVersionId方法
    那么 所有版本ID小于1002的Token应该被禁用
    而且 user1的V1 Token状态应该变为"禁用"
    而且 user2的V1 Token状态应该变为"禁用"
    而且 新生成的V2 Token应该为有效状态

  @negative @exception
  场景: 异常处理 - DM审批状态查询失败
    假如 数据库中存在审批单(审批单号="uuid-001")
    而且 DM系统不可用或返回错误
    当 用户调用审批刷新接口 approvalRefresh
    那么 系统应该捕获异常
    而且 系统应该记录错误日志
    而且 系统应该返回友好的错误提示
    而且 不应该生成任何Token

  @negative @exception
  场景: 异常处理 - Token生成失败
    假如 数据库中存在审批通过的审批单
    而且 JWT生成服务异常
    当 系统尝试生成Token
    那么 系统应该抛出ApiServiceTokenException
    而且 系统应该记录异常日志
    而且 数据库事务应该回滚
    而且 审批单状态应该保持不变

  @negative @exception
  场景: 异常处理 - 数据库保存失败
    假如 系统已生成Token对象列表
    而且 数据库连接异常
    当 系统尝试批量保存Token到数据库
    那么 系统应该抛出ApiServiceTokenException(错误码800002)
    而且 系统应该记录异常日志
    而且 事务应该回滚

  @negative @validation
  场景: 参数验证 - 提单参数缺失
    假如 用户尝试提单
    当 用户未填写审批单名称
    那么 系统应该返回参数错误提示
    而且 不应该创建审批记录

  @negative @validation
  场景: 参数验证 - 授权期限格式错误
    假如 用户尝试提单
    当 用户填写授权期限为非法格式(如"abc")
    那么 系统应该返回参数错误提示
    而且 不应该创建审批记录

  @negative @validation
  场景: 参数验证 - API版本不存在
    假如 用户尝试提单
    当 用户提供的API版本ID不存在
    那么 系统应该返回资源不存在错误
    而且 不应该创建审批记录

  @performance
  场景: 性能测试 - 大量用户Token生成
    假如 数据库中存在历史审批记录包含100个用户
    而且 用户创建新版本并申请新增50个用户
    当 审批通过后调用审批刷新接口
    那么 系统应该在合理时间内(如5秒)完成Token生成
    而且 系统应该生成150个Token(100历史+50新增)
    而且 所有Token应该正确保存到数据库

  @performance
  场景: 性能测试 - 批量API提单性能
    假如 用户尝试批量提单10个API
    而且 每个API都有历史授权用户
    当 用户提交批量提单请求
    那么 系统应该在合理时间内(如10秒)完成所有API的提单
    而且 每个API应该独立处理
    而且 系统应该返回统一的审批单号

  @security
  场景: 安全验证 - 权限控制
    假如 数据库中存在审批单(审批单号="uuid-001", 创建者="user_admin")
    而且 当前登录用户为"user_other"
    当 用户尝试调用approvalRefresh接口
    那么 系统应该验证用户权限
    而且 系统应该拒绝非创建者的访问
    而且 系统应该记录安全审计日志

  @security
  场景: 安全验证 - Token有效期验证
    假如 系统为用户生成30天有效期的Token
    当 Token生成成功
    那么 Token中应该包含过期时间信息
    而且 系统应该在Token过期后自动禁用
    而且 过期Token不应该能够访问API

  @audit
  场景: 审计追踪 - 审批单操作日志
    假如 用户提交审批单
    当 审批单创建成功
    那么 系统应该记录创建时间
    而且 系统应该记录创建者
    而且 系统应该记录申请用户列表
    而且 系统应该记录授权期限

  @audit
  场景: 审计追踪 - Token生成日志
    假如 系统开始生成Token
    当 Token生成完成
    那么 系统应该记录生成的Token数量
    而且 系统应该记录包含的历史用户数量
    而且 系统应该记录包含的新增用户数量
    而且 系统应该记录禁用的旧版本Token数量

  规则: 增量授权自动合并规则
    """
    系统在Token生成时自动执行增量授权:
    1. 自动查询历史审批单(getSecondApproval)
    2. 自动查询历史有效Token
    3. 对比申请用户列表,找出需要合并的历史用户
    4. 复制历史配置(IP白名单、访问限制等)
    5. 合并到当前Token列表
    6. 禁用旧版本Token
    7. 保存新版本Token
    """

  规则: 版本隔离规则
    """
    每个API版本有独立的Token集合:
    1. 新版本审批通过后,旧版本Token自动禁用
    2. 历史用户的Token需要在新版本中重新生成
    3. 版本ID用于区分Token所属版本
    4. 禁用操作基于版本ID对比(api_version_id < 当前版本ID)
    """

  规则: 防重复授权规则
    """
    避免重复生成Token:
    1. 检查审批单号+版本ID是否已存在Token
    2. 如果已存在,跳过重复插入
    3. 特殊处理DEFAULT_APPROVAL_NO("0001"),允许重复
    4. 返回成功状态,避免用户感知错误
    """